<div class="row text-center">
<div class="bg-primary">{{page.title}}</div>
{% for tagitem in site.tags %}
{% if check > 1 %}
<<div class="bg-info">{{tagitem[0]}} - {{tagitem[0].size}}</div>
{% endif %}
	{% for p in site.categories[{{include.key}}] reversed %}
      {% if p.tags contains tagitem[0] %}
		{% assign check = 1|plus:1 %}
		{% include image %}
      {% endif %}
	{% endfor %}
	{% assign check = 1 %}
{% endfor %}
</div>